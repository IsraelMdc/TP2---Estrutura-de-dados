#include "pacient.h"
#include <stdio.h>
#include <stdlib.h>

void main()
{
    int time_unit = 0;

    
    while (time_unit < 43200)
    {
        int gen_pacient_chance = gen_randint(0, 100);
        if (gen_pacient_chance <= 20)
        {
            Pacient *create_meliant(int time_unit);
            

        } 
        
        time_unit++;
    }
    

    
}